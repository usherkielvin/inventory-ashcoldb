# SQL Server / SSMS
*.bak
*.mdf
*.ldf
*.trn

# OS / IDE
.DS_Store
Thumbs.db
.vs/
*.user

# Secrets (never commit real passwords)
**/secrets.local.sql

# Node / Vite (optional web stack under web/)
web/**/node_modules/
web/**/dist/
web/**/.env
