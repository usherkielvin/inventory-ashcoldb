# Copy to .env or .env.local — must match Express listen port in ../server/.env (PORT=...)
# If you get EADDRINUSE on 3001, set server PORT=3002 and set this to that URL:
# VITE_API_PROXY_TARGET=http://localhost:3002

VITE_API_PROXY_TARGET=http://localhost:3001
